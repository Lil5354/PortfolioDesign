const db = require('better-sqlite3')('./UEFGallery.API/gallery.db'); const cats = ['Poster', 'Branding', 'UI/UX', '3D Art', 'Illustration', 'Typography', 'Photography', 'Packaging', 'Motion Design', 'Editorial']; const artworks = db.prepare('SELECT artwork_id FROM artworks').all(); const stmt = db.prepare('UPDATE artworks SET subject = ? WHERE artwork_id = ?'); db.transaction(() => { artworks.forEach((a, i) => stmt.run(cats[i % cats.length], a.artwork_id)); })(); console.log('Subjects fixed!');
