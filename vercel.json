{
  "framework": "nextjs",
  "buildCommand": "vite build && cp -r dist/* public/ && DATABASE_URL=postgresql://d:d@localhost:5432/d npx prisma generate && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next"
}
