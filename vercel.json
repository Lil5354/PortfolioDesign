{
  "framework": "nextjs",
  "buildCommand": "vite build && cp -r dist/* public/ && npx prisma generate && npx prisma db push --accept-data-loss && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next"
}
