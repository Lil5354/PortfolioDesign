{
  "framework": "nextjs",
  "buildCommand": "npx prisma db push --accept-data-loss && npx vite build && cp -r dist/. public/ && npm run build",
  "installCommand": "npm install",
  "outputDirectory": ".next"
}
